# Upsun configuration, generated using AI at: 2026-05-26T18:29:32.814Z
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  app-eswirdbesser:
    type: nodejs:24

    build:
      flavor: none

    hooks:
      build: |
        set -ex
        npm install --omit=dev
        npm run build

    web:
      commands:
        start: npm run web

      locations:
        /:
          root: public
          passthru: true
          index: [index.html]

    variables:
      env:
        NODE_ENV: production
